<Project Sdk="Godot.NET.Sdk/4.6.1">
  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>
    <RootNamespace>Playground</RootNamespace>
    <EnableDynamicLoading>true</EnableDynamicLoading>
    <LangVersion>12.0</LangVersion>
  </PropertyGroup>
  <ItemGroup>
    <!-- Local SDK checkout, pinned to the engine-agnostic flavor: the net8.0
         flavor is the test build and drags NUnit + global-namespace fixtures. -->
    <ProjectReference Include="../../../../colyseus-unity-sdk/nuget/Colyseus.csproj">
      <SetTargetFramework>TargetFramework=netstandard2.1</SetTargetFramework>
    </ProjectReference>
  </ItemGroup>
</Project>
